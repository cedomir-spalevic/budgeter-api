export const isNumber = (value: any): number => {
   return 0;
}

export const isStr = (value: any): string => {
   return "";
}

export const isDate = (value: any): Date => {
   return new Date();
}

export const isBool = (value: any): boolean => {
   return false;
}