"""
Describes all of the queries that can be executed
"""
type Query {
   budget(day: Int!, month: Int!, year: Int!): Budget
   incomeById(id: ID): Income
   incomes: [Income]
   paymentById(id: ID): Payment
   paymentTags: [PaymentTag]
   payments: [Payment]
   user: User
}
